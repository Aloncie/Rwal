## 💡 Rwal – Ideas & Future Enhancements

1. Add command‑line flags beyond `--change`, e.g., `--help`, `--version`.
2. Purity filter (NSFW/Sketchy) in API requests.
3. CLI preview using `libsixel` or just displaying the URL before setting wallpaper.
4. Download history (`history.json`) with a “back” command to restore previous wallpapers.
5. Abstract wallpaper setter for cross‑platform support (GNOME, Windows).
6. Option to save wallpaper to a user‑defined directory.
7. Rework all menus to 1 class
8. FIx terminal size menu bug. 
9. Add tests on core methods and logic.

Make fetchImage std::optinal func and edit it in WallpaperManager too.
#idea